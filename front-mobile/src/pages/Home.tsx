import React from "react";
import { Text, View } from "react-native";

const Home: React.FC = () => {
  return (
    <View>
      <Text>Tela de Login (tela temporária)</Text>
    </View>
  );
};

export default Home;
